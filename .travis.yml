language: python
python:
  - "2.7"

addons:
  apt:
    packages:
      - libhdf5-serial-dev

install:
  - pip install .
# command to install dependencies
script:
  - cd mirror_seq/tests/
  - python -m unittest discover
branches:
  only:
    - master
